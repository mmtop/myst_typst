version: 1
project:
  downloads:
    - id: thesis-pdf
  exports:
    - id: thesis-pdf
      format: typst
      template: ../
      output: ./exports/typst_template_example.pdf

# Template options for Typst export      
      show_cover_full: true
      show_title_page: true
      show_title_page_image: false
      show_contributor_affiliations: true

# Table of contents and lists of figures/tables options
      show_toc: true
      toc_depth: 2
      
      show_list_of_figures: false
      show_list_of_tables: false


# Bibliography options 
      show_bibliography: true     
      bibliography_title: Bibliography
      # Common built-in Typst styles include:
      # ieee | apa | mla | chicago-author-date | chicago-notes
      bibliography_style: chicago-author-date
      bibliography_numbered_heading: false

# Page layout and styling      
      # Default paper size from the template: a4
      # Common alternatives: us-letter, b5
      paper_size: a4
      # Margin defaults come from the template itself, but can be overridden here:
      margin_top_cm: 2.5
      margin_bottom_cm: 2.5
      margin_left_cm: 2.5
      margin_right_cm: 2.5

# Font options      
      # Font defaults come from the template itself:
      font_size_pt: 11
      line_spacing_em: .6

      # The following default fonts also come from the template itself.
      # Those fonts are always available for the typst compiler as built-in fallbacks,
      # but you can optionally override them 
      # if you have specific fonts installed on your system that you'd like to use. 
      # Just make sure to provide the correct font names 
      # as recognized by your local Typst installation.
      # 
      # font_body: Libertinus Serif
      # font_math: New Computer Modern Math
      # font_mono: DejaVu Sans Mono
      #
      # Instructions for the optional (recommended) bundled STIX setup:
      # 1. Make the bundled fonts visible to Typst.
      #
      #    Windows: Execute the line below in the Powershell from the "example" folder:
      #    $env:TYPST_FONT_PATHS = (Resolve-Path ..\src\assets\fonts)
      #
      #    MacOS/Linux/WSL: Execute the line below in bash/zsh from the "example folder:
      #    export TYPST_FONT_PATHS="$(cd ../src/assets/fonts)"
      #
      # 2. Then uncomment the overrides below:
      #     font_body: STIX Two Text
      #     font_math: STIX Two Math
      #
      # `font_mono` stays on the template default, 
      # but you can override it if needed following 
      # the same pattern as for the other two font options above.


      # Cover variant: simple | graphical | custom
      cover_page_variant: graphical
      # Recommended: replace the default template cover image with your own.
      # If omitted, the template's built-in cover image is used.
      # cover_background_image: content/assets/cover.jpg
      cover_title_box_opacity_pct: 55
      
      # Title-page variant: basic | formal | custom
      title_page_variant: "formal"
      # Optional title-page footer notes for both detailed variants:
      # show_title_page_cover_description: true
      # title_page_cover_description: A short description of the cover image.
      # show_title_page_confidentiality_statement: true
      # title_page_confidentiality_statement: This thesis is confidential and cannot be made public.
      ## Title-page image settings:
      # title_page_image: config/template_assets/figures/defaultcover.jpg
      # title_page_image_anchor: bottom
      # title_page_image_width_cm: 3
      # title_page_image_dx_cm: 0
      # title_page_image_dy_cm: 0


# Options:
      # Optional: override the default template logo on the title page
      # and on the graphical cover with your own file path.
      # If omitted, the template's built-in logo is used.
      # logo: content/assets/logo.svg
